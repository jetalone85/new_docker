<form method="post" action="{{ path('shared_afe_account_delete', {'id': afe_account.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ afe_account.id) }}">
    <button class="btn">Delete</button>
</form>