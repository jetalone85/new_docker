{% extends 'base.html.twig' %}

{% block title %}CompanyInvite{% endblock %}

{% block body %}
    <h1>CompanyInvite</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ company_invite.id }}</td>
            </tr>
            <tr>
                <th>Hash</th>
                <td>{{ company_invite.hash }}</td>
            </tr>
            <tr>
                <th>ConfirmedAt</th>
                <td>{{ company_invite.confirmedAt ? company_invite.confirmedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ company_invite.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('shared_company_invite_index') }}">back to list</a>

    <a href="{{ path('shared_company_invite_edit', {'id': company_invite.id}) }}">edit</a>

    {{ include('shared_company_invite/_delete_form.html.twig') }}
{% endblock %}