<form method="post" action="{{ path('shared_company_handshake_delete', {'id': company_handshake.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ company_handshake.id) }}">
    <button class="btn">Delete</button>
</form>